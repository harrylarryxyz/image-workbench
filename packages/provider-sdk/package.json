{
  "name": "@image-workbench/provider-sdk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": { "build": "tsc -p tsconfig.json", "typecheck": "tsc -p tsconfig.json --noEmit", "lint": "tsc -p tsconfig.json --noEmit", "test": "node --test dist/**/*.test.js" },
  "dependencies": { "@image-workbench/shared": "workspace:*", "zod": "latest" },
  "devDependencies": { "typescript": "latest" }
}
